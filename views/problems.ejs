<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Problems</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-purple-100">
    <%- include('partials/navbar.ejs') %>
    <main class="my-28">
        <div class="current_problems bg-white w-3/4 mx-auto my-10 p-10 rounded-xl flex flex-col items-center">
            <h1 class=" bg-fuchsia-600 text-xl font-bold text-white px-4 py-2 rounded-full ">CURRENT PROBLEMS</h1>
            <div class="mt-8 grid grid-cols-3 gap-14" id="grid">

                <%  for( let problem of problems) { %>
                    <% if(problem.isCompleted === false) {  %>
                    <div class="card">
                        <h1 class="username"><span><%= problem.name %></span><span>13:50</span></h1>
                        <p class="p-3"><%= problem.text %></p>
                        <center>
                        <button type="button" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-1 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">Edit</button>
                        <button type="button" class="focus:outline-none text-white bg-red-700 hover:bg-red-800 focus:ring-1 focus:ring-red-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-900">Delete</button></center>

                    </div>
                    <%  } %>
                <% } %> 
                
                <div class="border border-black rounded-xl overflow-hidden shadow-md">
                    <form class="flex flex-col items-center" id="entry">
                        <input class="w-full text-center p-3 bg-slate-100 font-semibold" type="text" name="username" id="" placeholder="@Username">
                        <textarea class="w-full text-center h-full p-3" name="problemtext" id="" cols="30" rows="4" placeholder="Enter your problem here"></textarea>
                        <button class="bg-purple-700 text-white px-4 py-1 border rounded-full hover:bg-purple-800 hover:text-white hover:border-purple-700">Add Problem</button>
                    </form>
                </div>
            </div>
        </div>
    </main>
    <hr class="border border-black">
    
    <main class="my-28">
        <div class="current_problems bg-white w-3/4 mx-auto my-10 p-10 rounded-xl flex flex-col items-center">
            <h1 class=" bg-fuchsia-600 text-xl font-bold text-white px-4 py-2 rounded-full ">PROBLEMS SOLVED</h1>
            <div class="mt-8 grid grid-cols-3 gap-14" id="grid">

                <%  for( let problem of problems) { %>
                    <% if(problem.isCompleted === true) {  %>
                    <div class="card">
                        <h1 class="username"><span><%= problem.name %></span></h1>
                        <p class="p-3"><%= problem.text %></p>
                    </div>
                    <%  } %>
                <% } %> 
                
            </div>
        </div>
    </main>
    <script src="/js/problems.js"></script>
</body>
</html>