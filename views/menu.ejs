<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hostel Mess Menu</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
    rel="stylesheet"
  />
  <style>
    .gradient {
      background: linear-gradient(90deg, #004aad 0%, #cb6ce6 100%);
    }
    .meal-item {
      padding: 0.5rem;
      border-bottom: 1px solid #e5e7eb; /* Light gray border between items */
    }
    .meal-item:last-child {
      border-bottom: none; /* Remove border for the last item */
    }
  </style>
</head>
<body class="bg-gray-50 font-['Poppins']">
  <%- include('partials/navbar.ejs') %>
  
  <div class="pt-16 min-h-screen">
    <!-- Header -->
    <div class="bg-gradient-to-r from-[#004aad] to-[#cb6ce6] shadow-lg">
      <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold text-white text-center">
          MESS MENU
        </h1>
      </div>
    </div>

    <!-- Main Content -->
    <main class=" mx-auto px-4 sm:px-6 lg:px-8 py-8 overflow-x-scroll">
      <% if (query.feedback === 'success') { %>
        <div class="mb-6 p-4 bg-green-100 border border-green-300 text-green-800 rounded-lg">
          Thank you for your feedback!
        </div>
      <% } %>
      
      <div class="flex flex-row w-fit gap-6">
        <% ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'].forEach(day => { %>
          <div class="bg-white w-[19rem] rounded-xl shadow-md hover:shadow-xl transition-shadow duration-300 p-6">
            <h3 class="text-xl w-full font-semibold text-center mb-6 text-gray-800 border-b pb-2"><%= day %></h3>
            
            <% ['Breakfast', 'Lunch', 'Snacks', 'Dinner'].forEach(meal => { %>
              <div class="mb-6 w-full">
                <!-- Meal Title -->
                <div class="border-l-4 border-blue-600 pl-3 mb-4">
                  <h4 class="text-lg font-medium text-gray-700"><%= meal %></h4>
                </div>
                
                <!-- Menu Items -->
                <div class="space-y-2 bg-gray-50 rounded-lg p-3">
                  <% menuItems.filter(item => item.day === day && item.mealType === meal).forEach(item => { %>
                    <div class="meal-item">
                      <div class="text-gray-600 text-sm">
                        <%= item.name %>
                        <% if(item.alternateWeek) { %>
                          <span class="text-gray-400 italic ml-2">(Alternate)</span>
                        <% } %>
                        <% if(item.seasonal) { %>
                          <span class="text-green-500 font-medium ml-2">(Seasonal)</span>
                        <% } %>
                      </div>
                    </div>
                  <% }); %>
                </div>

                <!-- Meal-specific Feedback -->
                <div class="mt-4 bg-gray-50 p-4 rounded-lg">
                  <form action="/feedback" method="POST" class="space-y-3">
                    <!-- Hidden inputs for day and mealType -->
                    <input type="hidden" name="day" value="<%= day %>">
                    <input type="hidden" name="mealType" value="<%= meal %>">
                    
                    <div>
                      <label class="block text-sm font-medium text-gray-700 mb-1">Rate this meal</label>
                      <select name="rating" class="w-full rounded-md border border-gray-300 px-3 py-2" required>
                        <option value="5">⭐⭐⭐⭐⭐</option>
                        <option value="4">⭐⭐⭐⭐</option>
                        <option value="3">⭐⭐⭐</option>
                        <option value="2">⭐⭐</option>
                        <option value="1">⭐</option>
                      </select>
                    </div>
                    
                    <div>
                      <label class="block text-sm font-medium text-gray-700 mb-1">Comments</label>
                      <textarea name="comment" rows="2" class="w-full rounded-md border border-gray-300 px-3 py-2"></textarea>
                    </div>
                    
                    <button type="submit" class="w-full bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 transition-colors">
                      Submit Feedback
                    </button>
                  </form>
                </div>
              </div>
            <% }); %>
          </div>
        <% }); %>
      </div>
    </main>
  </div>

  <%- include('partials/footer.ejs') %>
  <script src="/js/navbar.js"></script>
</body>
</html>
